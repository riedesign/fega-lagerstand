<?php
/**
 * Legacy-Datenansicht — wird nicht mehr ausgefuehrt.
 * Der Ordner ist per .htaccess gesperrt. Code bleibt nur als
 * Referenz fuer die Migration zur neuen Architektur (siehe
 * CLAUDE.md + CHANGELOG.md).
 *
 * Wenn du diese Datei irgendwann doch wieder brauchst, nutze
 * die ENV-basierte Config aus `/config/db_config.php`. Echte
 * Credentials gehoeren NIE in den Code.
 */
die('alteDatenansicht ist deaktiviert.');
